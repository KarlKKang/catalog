export const CSS_AUTO = 'auto';

const enum CSS_UNIT {
    PX = 'px',
    EM = 'em',
    PERCENT = '%',
    VH = 'vh',
}
export type { CSS_UNIT };
export const CSS_UNIT_PX = CSS_UNIT.PX;
export const CSS_UNIT_EM = CSS_UNIT.EM;
export const CSS_UNIT_PERCENT = CSS_UNIT.PERCENT;
export const CSS_UNIT_VH = CSS_UNIT.VH;

const enum CSS_CURSOR {
    NOT_ALLOWED = 'not-allowed',
}
export type { CSS_CURSOR };
export const CSS_CURSOR_NOT_ALLOWED = CSS_CURSOR.NOT_ALLOWED;